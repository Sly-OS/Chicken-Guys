Place website images in this folder.

Suggested use:
- Put menu item photos here
- Use simple names like `half-chicken.png` or `full-chicken.jpg`
- Tell me the filenames and I can wire them into the site

Current code also uses `/assets`, but this `/images` folder is now available as the main drop location for future menu photos.
